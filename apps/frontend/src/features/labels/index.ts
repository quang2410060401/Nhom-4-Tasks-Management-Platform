/**
 * Feature: Labels (Nhãn)
 * Quản lý CRUD labels cho mỗi group, gán label cho task.
 *
 * Cấu trúc sẽ bao gồm:
 * - api/        → API calls cho label endpoints
 * - components/ → LabelTag, LabelSelect, LabelForm...
 * - hooks/      → useLabels, useCreateLabel...
 * - schemas/    → Yup validation schemas
 * - types.ts    → Label-specific types
 */

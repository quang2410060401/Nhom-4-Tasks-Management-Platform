/**
 * Shared components — UI components dùng chung cho nhiều features.
 *
 * Chỉ đặt component ở đây nếu nó được dùng bởi >= 2 features.
 * Ưu tiên dùng component từ Ant Design trước khi tạo custom.
 */

export { AsyncState } from './common/AsyncState';
export { AppEmpty } from './common/AppEmpty';
export { AppLoading } from './common/AppLoading';
export { ConfirmAction } from './common/ConfirmAction';
export { PageHeader } from './common/PageHeader';
